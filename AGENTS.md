# AGENTS.md — 项目上下文与合规基线

## 项目概述

新长公司是创业园运营管理主体，向入驻企业提供经营办公场地租赁与证件代办配套服务。本系统为内部文员后台管理系统，用于房间资源、入驻企业、企业人员、证件代办资料导出的线上化管理，替代原有分散的 Excel 人工维护方式，实现业务全流程留痕、模板化文档导出，提升文员工作效率与数据准确性。

## 技术栈约束

| 层 | 技术 | 约束说明 |
|---|---|---|
| 后端语言 | Java 17 | — |
| 后端框架 | Spring Boot 3 | — |
| 数据库 | MySQL 8 | — |
| ORM | MyBatis-Plus | **优先不手写 SQL**，尽量使用 LambdaQueryWrapper / 注解方式 |
| 权限框架 | sa-token | 登录会话、角色鉴权（管理员 / 普通用户），接口层强制鉴权 |
| 前端 | Vue (Vue 3) | PC 后台管理，兼容移动端访问，支持深色模式，页面美观、具有科技感 |

## 模块地图

1. **房间管理模块** — 楼栋-楼层-房间资源维护，房间 CRUD，同一楼栋房间号唯一性约束，占用校验
2. **入驻公司管理模块** — 企业信息录入、场地绑定（一个企业可分配多个房间）、列表筛选、批量导入/导出、迁出/停办状态变更
3. **证件代办模板与资料导出模块** — 模板上传/删除，`#{字段名}` 变量替换，保持原格式批量导出代办文档并打包压缩下载
4. **入驻企业人员管理模块** — 人员信息维护、一对多附件上传（身份证/毕业证/学历报告）、花名册导出（Excel/Word）、任命书导出
5. **权限与日志模块** — sa-token 角色权限控制，关键业务操作日志记录（操作人、时间、内容）

## 合规基线（必须遵守）

### 权限安全

- 管理员拥有全部功能（增删改查导出）；普通用户仅可查看、导出，禁止新增/编辑/删除业务数据
- **接口层强制鉴权**（`@SaCheckRole` 或等效注解），不只依赖前端按钮隐藏
- 用户密码使用 **BCrypt** 加密存储
- 实体类使用lombak注解
- 关键代码操作要有注释

### 操作日志

- 房间、企业、人员的**关键业务操作**（新增、编辑、删除、状态变更）必须记录操作日志
- 日志字段：操作人、操作时间、操作内容（旧值 → 新值或变更描述）
- 日志数据保留 **6 个月**，超期可归档或定时清理

### 文件上传

- 校验文件格式（仅允许常见图片/PDF/Office 文档），**禁止可执行文件**（`.exe`, `.bat`, `.sh`, `.dll` 等）
- 校验文件大小，超限拒绝上传
- 人员附件支持更新覆盖，保留附件元数据

### 占位符规范

- 文档模板变量占位符统一为 **`#{字段名}`** 格式
- 系统找不到对应字段值时，导出处留空，日志记录缺失字段

## 声明

**本文件作为后续所有 Plan / Build 会话的默认上下文，任何开发决策不得偏离上述技术栈与合规基线。** 新增技术选型或违反合规的变更须经评审确认后方可实施。