# 产品需求文档(PRD)

## 新长入驻公司管理平台

> 技术栈：Java17 + SpringBoot3 + MySQL8 + MyBatis‑plus（优先不手写SQL） + sa‑token权限控制 文档版本：V0.1（初稿） 编写人：zichun 编写日期：2026‑09‑15 文档状态：草稿 

------

## 1 项目背景与问题现状

### 1.1 业务简介

新长公司为创业园运营管理主体，主要业务：向入驻企业提供经营办公场地租赁服务，同时为入驻企业提供各类证件代办配套服务。

### 1.2 当前现状与痛点

1. **入驻企业信息依靠Excel维护**：无统一业务系统，企业基础信息分散在多份Excel文件，查询、修改、版本管理困难，易出现信息不一致。
2. **证件代办材料人工重复制作**：大部分代办资料为固定模板，仅需替换企业名称、人员等变量信息；全部依靠人工复制粘贴修改文档，重复工作量大。
3. **缺少标准化流程**：房间资源、入驻企业、企业人员、代办业务没有线上流程，业务过程无留痕，出现问题难以回溯。
4. **人员附件材料管理混乱**：企业人员身份证、毕业证、学历报告等纸质/扫描件分散存储，查找更新不方便。
5. **导出花名册、代办资料人工整理，出错率高。**

### 1.3 目标用户

- 主要用户：公司内部业务文员，负责房间管理、入驻企业录入、人员维护、代办资料输出。
- 系统角色：管理员、普通查看用户。

### 1.4 典型使用场景

1. 业务文员收到新企业入驻申请，在系统录入企业信息，分配园区房间资源；
2. 维护入驻企业下的企业人员信息，上传身份证、毕业证、学历报告附件；
3. 需要办理证件代办业务，选择企业，基于模板批量导出填充好变量的全套代办文档；
4. 业务需要时导出企业人员花名册（Excel/Word）；
5. 企业迁出、停办业务，线上做企业状态变更；
6. 普通用户登录系统，仅可查询、导出数据，不可修改业务数据。

## 2 项目目标

### 2.1 业务目标

1. 实现园区房间资源线上管理；
2. 实现入驻企业全生命周期线上管理：入驻分配场地、信息变更、迁出停办；
3. 实现企业人员信息+附件统一管理，人员变动操作留痕；
4. 实现证件代办文档模板化，自动填充变量批量导出代办资料；
5. 建立权限体系区分管理员、普通用户，关键操作日志留痕。

### 2.2 可量化指标

1. 证件代办资料准备人工操作时间降低**50%**；
2. 企业信息、人员信息录入错误率降低99%；
3. 企业花名册、代办资料导出由小时级缩短至秒级；
4. 房间资源、企业人员业务操作全部系统留痕，可追溯。

## 3 项目范围（需求边界）

### 3.1 本期纳入实现功能

#### 3.1.1 房间管理模块

1. 园区楼栋楼层房间基础数据维护；园区共3栋楼，多楼层，楼层下挂载房间；
2. 房间CRUD：新增、修改房间号、删除、列表查询筛选；
3. 业务约束：同一楼栋内房间号不能重复；房间作为资源可分配给入驻企业。

#### 3.1.2 入驻公司管理模块

1. 企业信息录入；绑定分配园区房间场地；
2. 企业列表筛选查询、批量导入、批量导出；
3. 企业地址变更、迁出、停办状态变更业务；
4. 企业关键状态变更操作日志记录。

#### 3.1.3 证件代办模板与资料导出模块

1. 代办文档模板管理，模板无需系统生成，由人工编写后上传至系统指定的文件夹中，系统需要给出现有的模板列表的删除与新模版的上传操作；
2. 模板变量标记约定使用 `#{字段名}`，导出时自动替换系统中企业/人员对应字段，需要替换的变量标记可能存在于word文件的页面或者页脚中；
3. 保持原始文档格式不变，支持对指定入驻企业批量导出全套代办资料下载。可以提供压缩包下载。

#### 3.1.4 入驻企业人员管理模块

1. 企业人员信息维护：姓名、性别、身份证号、毕业学校、专业、手机号、照片、岗位；人员状态：在职/离职；
2. 人员附件一对多上传：身份证正反面、毕业证照片、学历认证报告PDF；支持附件更新替换；
3. 人员增删改查，人员在职/离职状态变更；人员变动操作留痕；
4. 花名册导出：支持Excel / Word，输出字段：姓名、毕业学校、专业、身份证号、照片，输出该企业全部人员。
5. 人员任命书导出：根据提供的任命书模板，导出所有人员的任命书。任命书中需要替换的文字同样使用#{字段名}标记

#### 3.1.5 权限与日志模块

1. sa‑token实现权限控制：管理员拥有全部功能；普通用户仅查看、导出，禁止新增/修改/删除；
2. 关键业务操作留存操作日志。

### 3.2 本期不包含（后续迭代，明确边界）

1. 园区房间平面图可视化展示、图形化房屋占用状态展示；

2. 代办业务流程审批流、代办任务进度跟踪；

3. 对外客户门户，仅对内文员后台系统；

4. 财务收费、合同管理；

   > 以上能力不在本次版本范围，后续版本评估后再纳入。

## 4 收益分析

### 4.1 业务价值

1. **效率提升**：代办材料、花名册不再人工复制整理，减少重复手工工作，提升文员工作效率；
2. **质量提升**：模板自动填充减少人工复制粘贴带来的错别字、信息错填，降低业务出错；
3. **资产沉淀**：房间资源、入驻企业、人员材料统一线上存储，告别分散Excel，信息可追溯；
4. **客户体验**：代办资料准备速度加快，提升入驻企业服务体验。

### 4.2 成本与效率节省

1. 人力工时节省：代办资料准备工时降低50%；
2. 纸质扫描件电子归档，减少文件查找时间；

### 4.3 间接收益

业务数据线上化，为后续统计报表、园区运营分析打下数据底座。

## 5 方案简述：技术选型简要思路

1. **后端**：Java17 + SpringBoot3，MyBatis‑plus，尽量避免手写SQL提升开发效率；
2. **权限框架**：sa‑token，实现登录会话、角色权限控制（管理员、普通用户）；
3. **数据库**：MySQL8，存储楼栋、楼层、房间、入驻企业、人员、附件元数据、操作日志；
4. **文件存储**：人员照片、身份证、毕业证、PDF学历报告 本地存储；
5. **文档处理**：解析带`#{字段名}`标记的模板，替换变量，保持原格式输出文档；支持Excel、Word导出花名册与代办材料；
6. **前端**：【技术栈vue】PC后台管理系统，兼容PC、移动端访问；支持深色模式。



## 6 资源评估

### 6.1 需要岗位与人力

| 岗位     | 人数 | 工作内容                                       |
| -------- | ---- | ---------------------------------------------- |
| 产品     | 0    | PRD细化、需求答疑、验收                        |
| 后端开发 | 1    | 数据库设计、接口开发、权限、文档导出、文件处理 |
| 前端开发 | 0    | 页面开发，列表、表单、导入导出、深色模式       |
| 测试     | 0    | 功能测试、边界场景、导出文件校验、权限测试     |
| 运维     | 0    | 部署、数据库初始化、文件存储环境准备           |

### 6.2 预估周期

总周期：7工作日；

- 需求评审 & 数据库设计：1
- 开发实现：3
- 测试、bug修复：2
- 上线部署：1

> 说明：周期受人力投入、业务确认速度影响，为预估值。

## 7 风险识别与应对预案

### 7.1 业务风险

| 风险描述                                 | 风险等级 | 应对预案                                                     |
| ---------------------------------------- | -------- | ------------------------------------------------------------ |
| 业务方代办模板格式繁多，模板解析兼容问题 | 中       | 先梳理现有真实模板样本，限定支持模板格式；不兼容模板业务方做格式标准化；前期收集样例做测试用例。 |
| 业务规则口头描述，开发过程频繁变更需求   | 高       | 需求评审确认全部业务规则；变更走需求变更流程，评估影响范围与工期。 |
| 历史Excel存量数据导入，脏数据多          | 中       | 提供导入模板，导入做数据校验；业务方先清洗历史数据再批量导入。 |

### 7.2 技术风险

| 风险描述                           | 风险等级 | 应对预案                                                     |
| ---------------------------------- | -------- | ------------------------------------------------------------ |
| Word文档变量替换后原有排版格式错乱 | 高       | 严格测试真实业务模板；限定模板标记规范，提供业务制作模板指引；保留原始模板备份。 |
| 附件文件量大，存储方案不确定       | 中       | 技术评审确定存储方案；做好附件备份机制；记录附件元数据与关联关系。 |
| sa‑token权限逻辑理解偏差，越权操作 | 中       | 测试重点覆盖权限用例；管理员、普通用户角色逐条验证接口权限。 |

### 7.3 进度风险

| 风险描述                                 | 风险等级 | 应对预案                                              |
| ---------------------------------------- | -------- | ----------------------------------------------------- |
| 业务确认反馈慢阻塞开发                   | 中       | 约定业务答疑响应时效；关键节点安排业务方及时确认。    |
| 文档导出、文件处理出现复杂问题耗时超预期 | 中       | 前期做技术预研POC，验证文档导出可行性；预留缓冲时间。 |

------

# 附录

## 附录A 详细功能需求

### 模块一：房间管理

#### 功能描述

维护园区楼栋‑楼层‑房间资源，支持增删改查，为入驻企业分配经营场地。

| 编号  | 功能点           | 优先级 | 说明                                                         |
| ----- | ---------------- | ------ | ------------------------------------------------------------ |
| F1‑01 | 楼栋、楼层维护   | 高     | 新增、编辑、删除楼栋与楼层；楼层归属指定楼栋                 |
| F1‑02 | 房间新增         | 高     | 选择楼栋、楼层；录入房间号；同一楼栋房间号不能重复；房间状态【空闲/已分配/禁用】 |
| F1‑03 | 房间编辑         | 高     | 可修改房间号；校验同楼栋房间号唯一；修改记录日志             |
| F1‑04 | 房间删除         | 高     | 已分配给企业的房间不允许直接删除，需要先解除分配             |
| F1‑05 | 房间列表查询筛选 | 高     | 按楼栋、楼层、房间号、状态筛选；分页列表                     |

业务规则：

1. 楼栋下包含多个楼层，楼层下包含多个房间；
2. 同一楼栋，房间号唯一；
3. 房间已经分配入驻企业，不允许直接删除房间；
4. 删除楼栋/楼层时，若下面存在房间，需要提示，禁止删除有子数据的记录。

异常处理：

1. 新增/修改房间号，同楼栋重复：弹窗提示“该楼栋下房间号已存在，请修改”；
2. 删除已被占用房间：提示“房间已分配企业，不可删除，请先解除分配”；
3. 删除含有子楼层的楼栋：拦截，提示存在楼层数据。

### 模块二：入驻公司管理

#### 功能描述

管理入驻企业信息，完成场地分配、信息维护、批量导入导出、迁出停办状态管理。

| 编号  | 功能点              | 优先级 | 说明                                                         |
| ----- | ------------------- | ------ | ------------------------------------------------------------ |
| F2‑01 | 入驻企业新增        | 高     | 录入企业基础信息，可绑定分配房间资源；一个企业支持分配多个房间 |
| F2‑02 | 入驻企业编辑        | 高     | 修改企业基础信息，变更分配房间，变更地址                     |
| F2‑03 | 企业列表筛选查询    | 高     | 分页，支持多条件筛选，名称、房间号、楼和楼层筛选             |
| F2‑04 | 企业批量导入        | 高     | 下载导入模板，上传Excel导入，导入给出错误行提示              |
| F2‑05 | 企业批量导出        | 高     | 列表数据导出Excel                                            |
| F2‑06 | 企业迁出 / 停办操作 | 高     | 变更企业业务状态；记录操作人、操作时间、备注；迁出后房间释放为空闲 |

业务规则：

1. 企业绑定房间后，房间状态变更为已分配；企业迁出，释放房间资源；
2. 企业迁出、停办属于关键业务动作，强制记录操作日志；
3. 一个企业支持分配多个房间。

异常处理：

1. 批量导入格式错误，返回行号+错误原因；部分成功部分失败模式；
2. 迁出企业校验：校验企业下人员状态需要同步处理为“公司停办”

### 模块三：证件代办模板与资料导出

#### 功能描述

基于标记`#{字段名}`的模板，替换企业/人员字段，原样格式批量导出代办文档。

| 编号  | 功能点                   | 优先级 | 说明                                                         |
| ----- | ------------------------ | ------ | ------------------------------------------------------------ |
| F3‑01 | 选择企业导出代办全套资料 | 高     | 选定入驻企业，读取对应模板，替换#{变量}；保持原始文档格式不变，打包下载 |
| F3‑02 | 模板管理                 | 高     | 模板上传、删除；                                             |

业务规则：

1. 文档模板中占位符格式严格为`#{字段名}`；
2. 系统找不到对应字段值时，导出该处留空；
3. 导出不能修改原始模板文件，只做内存渲染输出；
4. 多个文档输出为压缩包下载。

异常处理：

1. 模板文件损坏，提示模板异常；
2. 变量字段不存在，导出内容该位置为空，日志记录缺失字段；
3. 大文档导出超时保护。

### 模块四：入驻企业人员管理

#### 功能描述

维护企业下人员信息、多附件管理，人员状态在职离职，人员变动留痕，花名册导出Excel/Word。

| 编号  | 功能点             | 优先级 | 说明                                                         |
| ----- | ------------------ | ------ | ------------------------------------------------------------ |
| F4‑01 | 新增企业人员       | 高     | 归属某入驻企业；录入：姓名、性别、身份证号、毕业学校、专业、手机号、照片、岗位；默认状态在职 |
| F4‑02 | 编辑人员信息       | 高     | 修改人员基础信息；状态切换在职/离职；状态变更记录操作日志    |
| F4‑03 | 人员删除           | 高     | 删除人员记录，删除操作留痕；附件文件：不删除磁盘文件         |
| F4‑04 | 人员列表查询筛选   | 高     | 企业维度过滤；按在职/离职筛选；分页                          |
| F4‑05 | 人员附件上传       | 高     | 一个人员多附件：身份证正反面、毕业证照片、学历认证报告PDF；支持更新覆盖旧附件；记录附件名称、上传时间 |
| F4‑06 | 人员附件下载预览   | 中     | 查看上传的图片、PDF附件                                      |
| F4‑07 | 导出企业人员花名册 | 高     | 选择企业，导出Excel或Word；字段：姓名、毕业学校、专业、身份证号、照片；输出全部在职人员 |

业务规则：

1. 人员归属于某一家入驻企业；
2. 人员状态变更（在职↔离职）必须记录变动日志（操作人、时间、旧状态、新状态）；
3. 附件一对多，支持重复上传更新覆盖；保留附件元数据记录。

异常处理：

1. 上传非允许文件格式拦截；
2. 文件大小超限拦截；
3. 花名册导出，人员照片缺失，照片位置留空。

### 模块五：权限与日志模块

#### 功能描述

sa‑token实现角色权限控制，记录关键业务操作日志。

| 编号  | 功能点           | 优先级 | 说明                                                         |
| ----- | ---------------- | ------ | ------------------------------------------------------------ |
| F5‑01 | 登录鉴权         | 高     | sa‑token登录会话控制；密码加密存储                           |
| F5‑02 | 管理员角色       | 高     | 全部增删改查导出权限                                         |
| F5‑03 | 普通用户角色     | 高     | 仅查看、导出；禁止新增、编辑、删除业务数据；接口层面做权限校验，不只前端隐藏按钮 |
| F5‑04 | 业务操作日志记录 | 高     | 房间、企业、人员关键变更，记录操作人、时间、操作内容；日志数据保留6个月 |

业务规则：

1. 权限前后端双重校验；前端做按钮控制，后端接口强制鉴权；
2. 操作日志保存周期6个月。

## 附录B 非功能需求

### 性能要求

| 项目           | 指标            |
| -------------- | --------------- |
| 页面加载时间   | ≤3s             |
| 并发在线用户   | 支持5人同时在线 |
| 接口响应时间   | 95%请求 ≤500ms  |
| 文件导出大文档 | 超时阈值5分钟   |

### 安全要求

1. 用户密码加密存储；
2. 接口权限校验，防止越权访问；
3. 上传文件做格式、大小校验，禁止可执行文件；
4. 业务操作日志至少保留6个月。

### 兼容性

1. PC浏览器：Chrome、Safari最近两个版本；
2. 支持移动端浏览器访问；
3. UI支持深色模式。

## 附录C 变更记录

| 版本 | 日期       | 修改人 | 变更内容            |
| ---- | ---------- | ------ | ------------------- |
| V0.1 | 2026‑09‑15 | zichun | 初稿，完整PRD评审版 |

> 后续版本在此追加记录。

------

如果你需要，我可以下一步帮你输出：

1. 数据库设计（MySQL建表语句，楼栋、楼层、房间、企业、人员、附件、操作日志表）；
2. 或者精简一份用于内部立项PPT提纲。